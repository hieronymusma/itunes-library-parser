# iTunes Library Title extractor

This project parses a `iTunes Library.xml` file and prints out every title in every playlist.

## Usage

To see an example execute `cargo run --release -- iTunes\ Library.xml`
